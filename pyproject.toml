[build-system]
requires = ["setuptools>=77.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-brain-mcp"
version = "1.0.1"
description = "ContextOS: document retrieval and conversation memory through MCP and FastAPI"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    {name = "Anudeep Adiraju"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
keywords = ["mcp", "claude", "ai", "semantic-search", "rag", "knowledge-base"]
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic-settings",
    "langchain-text-splitters",
    "python-multipart",
    "audioop-lts; python_version >= '3.13'",
    "langchain-google-genai",
    "langchain-pinecone",
    "pinecone",
    "langchain-anthropic",
    "langchain-core",
    "PyPDF2",
    "pytesseract",
    "Pillow",
    "SpeechRecognition",
    "pydub",
    "mcp>=1,<2",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "black",
    "flake8",
    "mypy",
]

[project.scripts]
personal-brain-mcp = "personal_brain_mcp.server:main"

[project.urls]
Homepage = "https://github.com/anudeepadi/personal-brain-mcp"
Repository = "https://github.com/anudeepadi/personal-brain-mcp"
Issues = "https://github.com/anudeepadi/personal-brain-mcp/issues"
Documentation = "https://github.com/anudeepadi/personal-brain-mcp/blob/main/docs/SETUP.md"
Changelog = "https://github.com/anudeepadi/personal-brain-mcp/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["."]
include = ["personal_brain_mcp*"]

[tool.setuptools.package-data]
personal_brain_mcp = ["*.md", "*.json", "*.txt"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
